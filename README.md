# TicketMaster Mobile App 🎟️

Мобилно приложение за Android за разглеждане на събития и управление на билети, разработено с **Kotlin** и **Jetpack Compose**.

## 💡 Идея
Целта на приложението е да предостави лесен и бърз начин за потребителите да откриват интересни събития и да запазват своите места дигитално. Чрез елиминиране на хартиените билети, TicketMaster генерира уникални дигитални пропуски със защитени QR кодове, които се съхраняват локално на устройството за офлайн достъп.

## 🚀 Как работи
Приложението функционира върху **SQLite** база данни, управлявана чрез `DatabaseHelper`. При първоначално стартиране, системата проверява дали базата е празна и автоматично я попълва с демонстрационни събития (`MockData`).

Основният работен процес се случва в реално време:
1.  При заявка за билет, приложението извлича списък с вече заетите места за конкретното събитие чрез SQL заявка (`getTakenSeats`).
2.  Алгоритъм генерира случайно място и го валидира спрямо заетите; ако е свободно, го резервира.
3.  Генерира се уникален идентификатор (UUID) и QR код, използвайки библиотеката **ZXing**, който кодира ID-то, името на събитието и мястото.
4.  Данните се записват транзакционно в таблица `tickets`.
5.  В секция "Моите билети", приложението изпълнява `JOIN` заявка между таблиците `events` и `tickets`, за да визуализира пълната информация за билета заедно с детайлите за събитието.

## 🏗️ Архитектура (MVVM)
Приложението следва принципите на **MVVM (Model-View-ViewModel)** архитектурата, адаптирана за Declarative UI:

*   **Model (Data Layer):**
    *   `Event` и `Ticket` data класове дефинират структурата на данните.
    *   `DatabaseHelper` играе ролята на Repository, управлявайки директните SQL операции (CRUD).
*   **View (UI Layer):**
    *   Изграден изцяло с **Jetpack Compose**. Екраните (`EventListScreen`, `TicketDetailsScreen`) са реактивни и се прерисуват автоматично при промяна на състоянието.
*   **ViewModel (Logic):**
    *   Логиката за състоянието (State Management) е внедрена в Composable функциите, използвайки `remember` и `mutableStateOf`, като `MainActivity` оркестрира навигацията и потока на данните.

## 📱 Потребителски поток

1.  **Разглеждане:** Скролване през списък със събития с техните дати, локации и цени.
2.  **Детайли:** Преглед на пълното описание на избрано събитие.
3.  **Покупка:** Натискане на бутон "Get Ticket" -> Автоматично генериране на място и QR код.
4.  **Портфейл:** Достъп до всички закупени билети в секция "My Tickets".
5.  **Валидация:** Отваряне на конкретен билет за визуализация на големия QR код за вход.

## 🛠️ Технологии и Версии

| Технология | Версия | Предназначение |
| :--- | :--- | :--- |
| **Android SDK** | 36 (UpsideDownCake) | Target Framework |
| **Kotlin** | 1.9.0+ | Основен език |
| **Jetpack Compose** | BOM 2024.02.01 | UI Toolkit |
| **Material Design 3** | 1.2.1 | Дизайн система |
| **SQLite** | Native | Локална база данни |
| **ZXing Core** | 3.5.3 | Генериране на QR кодове |
| **Gradle** | 8.2 | Build система |

## 🏁 Стъпки за стартиране

1.  Клонирайте хранилището:
    ```bash
    git clone https://github.com/iliev04/MobileApps2025-2301321062.git
    ```
2.  Отворете проекта в **Android Studio Iguana** (или по-нова версия).
3.  Синхронизирайте Gradle dependencies (`File -> Sync Project with Gradle Files`).
4.  Изберете емулатор (препоръчително Pixel 6 API 34+) или свържете физическо устройство.
5.  Стартирайте чрез зеления бутон **Run** (`Shift + F10`).

## 📦 Инсталация

Можете да инсталирате приложението директно чрез APK файла:
👉 [Свали app-release.apk](./apk/app-release.apk)
